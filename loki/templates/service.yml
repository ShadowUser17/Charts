apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: "{{ .Release.Name }}-{{ .Chart.Name }}-read"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-{{ .Chart.Name }}-read"
  ports:
    - name: "http"
      port: 3100
      targetPort: 3100

---
apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: "{{ .Release.Name }}-{{ .Chart.Name }}-write"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-{{ .Chart.Name }}-write"
  ports:
    - name: "http"
      port: 3100
      targetPort: 3100

---
apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: "{{ .Release.Name }}-{{ .Chart.Name }}-gw"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-{{ .Chart.Name }}-gw"
  ports:
    - name: "http"
      port: 3100
      targetPort: 3100

---
apiVersion: "v1"
kind: "Service"
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: "{{ .Release.Name }}-{{ .Chart.Name }}-join"
spec:
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-{{ .Chart.Name }}-join"
  ports:
    - name: "join"
      port: 7946
      targetPort: 7946
